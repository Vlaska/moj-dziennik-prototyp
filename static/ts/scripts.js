function init() {
    var grade_container = $('#grade-container');
    for (var _i = 0, _a = grade_container.children(); _i < _a.length; _i++) {
        var i = _a[_i];
        console.log(i);
    }
}
/**
 *
 * @param {HTMLElement} src
 */
function set_grade(src) {
    var selection = $('#selected-grade');
    console.log(selection);
}
